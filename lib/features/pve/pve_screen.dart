// PvEScreen: Campaign and AI battles
import 'package:flutter/material.dart';

class PvEScreen extends StatelessWidget {
  const PvEScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('PvE Campaign')),
      body: Center(child: Text('PvE UI Placeholder')),
    );
  }
}
